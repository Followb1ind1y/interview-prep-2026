# 划词翻译用。去 https://console.anthropic.com/settings/keys 建一个，
# 然后 cp .env.example .env.local 并填进去。.env.local 不会进 git。
ANTHROPIC_API_KEY=

# 可选：换模型。默认 claude-haiku-4-5（快、便宜，术语翻译够用）。
# 想要更准可以换 claude-sonnet-5 或 claude-opus-5，代价是每次多等几秒。
# TRANSLATE_MODEL=claude-haiku-4-5
