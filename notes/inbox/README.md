# Inbox

对话总结的中转站，不上网站、不进 git。

只有你明确说「总结这次对话」时，Capture skill 才会往这里写一篇 markdown。没说就丢掉，下次新对话也不会提醒。

- 知识类：确认后用项目里的 Publish skill（说「归档到 Docs」）合并进 `contents/docs/`
- 工作类：只留在这里，不要发布到个人网站

会话稿文件名：`YYYY-MM-DD-短标题.md`。本 README 会提交；`*.md` 会话稿被 `.gitignore` 忽略。
